import { LogsDirective } from './logs.directive';

describe('LogsDirective', () => {
  it('should create an instance', () => {
    const directive = new LogsDirective();
    expect(directive).toBeTruthy();
  });
});
