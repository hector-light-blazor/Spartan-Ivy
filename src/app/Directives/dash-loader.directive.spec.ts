import { DashLoaderDirective } from './dash-loader.directive';

describe('DashLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DashLoaderDirective(null);
    expect(directive).toBeTruthy();
  });
});
